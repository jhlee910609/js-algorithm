function solution(a, b) {
  const temp = [...a];

  let lastIdx = -1;
  let ans = "YES";
  let cur = temp.shift();

  for (let i = 0; i < b.length; i++) {}

  return ans;
}

console.log(solution("CBA", "CADBGE") === "YES");
