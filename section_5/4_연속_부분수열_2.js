function solution(input, M) {
  const len = input.lenght;
  let answer = 0;
  const sum = 0;
  const start = 0;

  for (let rt = 0; rt < len; rt++) {
    while (sum > M) {}
  }
  return answer;
}

console.log(solution());
